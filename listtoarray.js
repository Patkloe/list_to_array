// obtenir les proprietes d'un objet, (to get object properties.

/*  recup keys d'un object
function nbre_obj(){
 var j = 0;
 var tab = [];
 var test = {"a":1, "b":2 ,3:4};
 //var recup = Object.keys(test);
 //return recup.length;
 for(var i in test){
    tab[tab.length] = i;
    j++;
 }
 return tab;
}
nbre_obj(); */
